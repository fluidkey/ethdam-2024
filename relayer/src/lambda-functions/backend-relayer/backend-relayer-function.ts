// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for BackendRelayerFunction
 */
export interface BackendRelayerFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/lambda-functions/backend-relayer/backend-relayer.
 */
export class BackendRelayerFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: BackendRelayerFunctionProps) {
    super(scope, id, {
      description: 'src/lambda-functions/backend-relayer/backend-relayer.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/lambda-functions/backend-relayer/backend-relayer.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}